[project]
name = "coolwebsite-api"
version = "0.1.0"
description = "Toy FastAPI backend behind gerrymander"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
